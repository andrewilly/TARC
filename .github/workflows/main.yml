name: Build TARC Cross-Platform

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: tarc-linux-x86_64
            executable: tarc
            cmake_arch_flags: ""

          - os: windows-latest
            artifact_name: tarc-windows-x64.exe
            executable: tarc.exe
            cmake_arch_flags: "-A x64"

          - os: macos-latest
            artifact_name: tarc-macos-universal
            executable: tarc
            cmake_arch_flags: "-DCMAKE_OSX_ARCHITECTURES='x86_64;arm64'"

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake ninja-build liblzma-dev libxxhash-dev

    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install cmake ninja xz xxhash

    - name: Install dependencies (Windows)
      if: runner.os == 'Windows'
      run: |
        choco install cmake ninja
        echo "Per Windows, le librerie LZMA e XXHASH devono essere incluse nel progetto come codice sorgente"
        echo "oppure installate manualmente. Per ora continuiamo senza."

    - name: Setup MSVC (Windows)
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1

    - name: Configure CMake (Linux/Windows)
      if: runner.os != 'macOS'
      shell: bash
      run: |
        mkdir -p build
        cd build
        cmake .. \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_CXX_STANDARD=17 \
          -DCMAKE_C_STANDARD=11 \
          -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
          ${{ matrix.cmake_arch_flags }}

    - name: Configure CMake (macOS)
      if: runner.os == 'macOS'
      shell: bash
      run: |
        mkdir -p build
        cd build
        cmake .. \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_CXX_STANDARD=17 \
          -DCMAKE_C_STANDARD=11 \
          -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
          -DCMAKE_OSX_ARCHITECTURES="x86_64;arm64"

    - name: Build Project
      shell: bash
      run: |
        cd build
        cmake --build . --config Release --parallel 2

    - name: Find executable (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        if [ -f "build/tarc" ]; then
          echo "EXECUTABLE_PATH=build/tarc" >> $GITHUB_ENV
        elif [ -f "build/Release/tarc" ]; then
          echo "EXECUTABLE_PATH=build/Release/tarc" >> $GITHUB_ENV
        else
          find build -name "tarc" -type f | head -1 | xargs -I {} echo "EXECUTABLE_PATH={}" >> $GITHUB_ENV
        fi

    - name: Find executable (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        if (Test-Path "build/Release/tarc.exe") {
          echo "EXECUTABLE_PATH=build/Release/tarc.exe" >> $env:GITHUB_ENV
        } elseif (Test-Path "build/tarc.exe") {
          echo "EXECUTABLE_PATH=build/tarc.exe" >> $env:GITHUB_ENV
        } else {
          Get-ChildItem -Path build -Filter "tarc.exe" -Recurse |
          Select-Object -First 1 |
          ForEach-Object { echo "EXECUTABLE_PATH=$($_.FullName)" >> $env:GITHUB_ENV }
        }

    - name: Test executable
      shell: bash
      run: |
        chmod +x ${{ env.EXECUTABLE_PATH }} 2>/dev/null || true
        ${{ env.EXECUTABLE_PATH }} -h || true

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: ${{ env.EXECUTABLE_PATH }}
        if-no-files-found: error

  create-release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Prepare release files
      run: |
        VERSION=${GITHUB_REF#refs/tags/}
        echo "📦 Versione: $VERSION"

        find artifacts -type f -name "tarc-linux*" | head -1 | xargs -I {} cp {} tarc-$VERSION-linux-x86_64
        find artifacts -type f -name "tarc-windows*" | head -1 | xargs -I {} cp {} tarc-$VERSION-windows-x64.exe
        find artifacts -type f -name "tarc-macos*" | head -1 | xargs -I {} cp {} tarc-$VERSION-macos-universal

        chmod +x tarc-$VERSION-* 2>/dev/null || true

        echo "✅ File pronti:"
        ls -lh tarc-$VERSION-* || echo "❌ Nessun file trovato"

    - name: Generate checksums
      run: |
        sha256sum tarc-* > checksums.txt
        cat checksums.txt

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          tarc-*
          checksums.txt
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
