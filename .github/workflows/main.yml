name: Build TARC Cross-Platform

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x86_64
          - os: ubuntu-latest
            artifact_name: tarc-linux-x86_64
            executable: tarc
            cmake_arch_flags: ""
          
          # Windows x64
          - os: windows-latest
            artifact_name: tarc-windows-x64.exe
            executable: tarc.exe
            cmake_arch_flags: "-A x64"
          
          # macOS Intel (x86_64) - usa macos-latest con architettura forzata
          - os: macos-latest
            artifact_name: tarc-macos-intel
            executable: tarc
            cmake_arch_flags: "-DCMAKE_OSX_ARCHITECTURES=x86_64"
            # Forza l'uso di Rosetta per compilare per Intel
            extra_env: "ARCHFLAGS=-arch x86_64"

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: false

    - name: Install dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake ninja-build

    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install cmake ninja

    - name: Setup MSVC (Windows)
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1

    - name: Setup macOS Intel architecture
      if: runner.os == 'macOS' && matrix.cmake_arch_flags == '-DCMAKE_OSX_ARCHITECTURES=x86_64'
      run: |
        echo "Configurazione per macOS Intel x86_64..."
        # Forza l'uso di Rosetta 2
        sudo softwareupdate --install-rosetta --agree-to-license || true
        # Imposta variabili d'ambiente per architettura Intel
        echo "SDKROOT=$(xcrun --sdk macosx --show-sdk-path)" >> $GITHUB_ENV
        echo "MACOSX_DEPLOYMENT_TARGET=10.15" >> $GITHUB_ENV
        echo "CMAKE_OSX_DEPLOYMENT_TARGET=10.15" >> $GITHUB_ENV

    - name: Configure CMake
      shell: bash
      run: |
        mkdir -p build
        cd build
        cmake .. \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_CXX_STANDARD=17 \
          -DCMAKE_C_STANDARD=11 \
          -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
          ${{ matrix.cmake_arch_flags }}

    - name: Build Project
      shell: bash
      run: |
        cd build
        cmake --build . --config Release --parallel 2

    - name: Find executable (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        if [ -f "build/tarc" ]; then
          echo "EXECUTABLE_PATH=build/tarc" >> $GITHUB_ENV
        elif [ -f "build/Release/tarc" ]; then
          echo "EXECUTABLE_PATH=build/Release/tarc" >> $GITHUB_ENV
        else
          find build -name "tarc" -executable -type f | head -1 | xargs -I {} echo "EXECUTABLE_PATH={}" >> $GITHUB_ENV
        fi

    - name: Find executable (Windows)
      if: runner.os == 'Windows'
      run: |
        if (Test-Path "build/Release/tarc.exe") {
          echo "EXECUTABLE_PATH=build/Release/tarc.exe" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        } elseif (Test-Path "build/tarc.exe") {
          echo "EXECUTABLE_PATH=build/tarc.exe" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        } else {
          Get-ChildItem -Path build -Filter "tarc.exe" -Recurse | Select-Object -First 1 | ForEach-Object { echo "EXECUTABLE_PATH=$($_.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append }
        }
      shell: pwsh

    - name: Verify architecture (macOS)
      if: runner.os == 'macOS'
      shell: bash
      run: |
        echo "Verificando architettura del binario..."
        file ${{ env.EXECUTABLE_PATH }}
        if file ${{ env.EXECUTABLE_PATH }} | grep -q "x86_64"; then
          echo "✅ Binario Intel x86_64 compilato con successo"
        else
          echo "⚠️ Il binario non è x86_64, potrebbe essere ARM64"
          echo "Architettura rilevata:"
          file ${{ env.EXECUTABLE_PATH }}
        fi

    - name: Test executable
      shell: bash
      run: |
        chmod +x ${{ env.EXECUTABLE_PATH }} 2>/dev/null || true
        ${{ env.EXECUTABLE_PATH }} -h || true

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: ${{ env.EXECUTABLE_PATH }}
        if-no-files-found: error

  create-release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: List artifacts
      run: |
        echo "📦 Artifacts scaricati:"
        ls -la artifacts/
        echo ""
        echo "📁 Dettaglio file:"
        find artifacts -type f -exec ls -lh {} \;

    - name: Prepare release files
      run: |
        # Rinomina i file per la release
        cp artifacts/tarc-linux-x86_64/tarc ./tarc-linux-x86_64 2>/dev/null || true
        cp artifacts/tarc-windows-x64.exe/tarc.exe ./tarc-windows-x64.exe 2>/dev/null || true
        cp artifacts/tarc-macos-intel/tarc ./tarc-macos-intel 2>/dev/null || true
        
        # Rendi eseguibili i file Unix
        chmod +x tarc-linux-x86_64 2>/dev/null || true
        chmod +x tarc-macos-intel 2>/dev/null || true
        
        echo "✅ File preparati per la release:"
        ls -lh tarc-* || echo "Nessun file trovato"

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          tarc-linux-x86_64
          tarc-windows-x64.exe
          tarc-macos-intel
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
